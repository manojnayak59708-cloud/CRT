// temperature conversion
// Take input in the form of celcius and convert it into fahrenheit....

#include<iostream>
using namespace std;

int main(){
    
    double celcius;
    double fahrenheit;
    cin >> celcius;
    fahrenheit = (celcius * 9.0 / 5.0) + 32.0;
    cout << fahrenheit << endl;
    
    return 0;
}


 // GLOBAL VS LOCAL VARIABLE

#include<iostream>
using namespace std;


// global variable
int globalVariable = 100;
int main(){
    // local variable 
    int userInput;
    float result;
    
    cin >> userInput;
    result = (double)globalVariable / userInput;
    
    cout << result <<endl;
    return 0;
    
}






// problem 3 
// Logical and relational Operators 

#include<iostream>
using namespace std;

int main() {
    int side1, side2, side3;
    cin >>side1 >> side2 >>side3;
    
    bool isEqui = (side1 > 0) && (side1 == side2) && (side2 == side3);
    cout << isEqui << endl;
    return 0;
    
}








// problem 4 
// swap values of 2 varaibles using 3rd variable 

#include<iostream>
using namespace std; 

int main () {
    int a;
    int b;
    
    cin >>a >>b;
    //swapping Logic
    int temp = a;
    a = b;
    b = temp;
    
    cout <<a << "\n" << endl;
    return 0;
    
}







// Area of Shapes
// 1 -- circle, 2 == rectangle, 3-- triangle
#include<iostream>
using namespace std;

int main() {
    int choice;
    cin >> choice;
    
    if(choice == 1){
        double radius;
        cin >> radius;
        cout << 3.14 * radius * radius << endl;
    } else if (choice == 2) {
        double length, width;
        cin >> length >> width;
        cout << length * width<< endl;
    } else if (choice == 3) {
        double base, height;
        cin >> base >> height;
        cout << 0.5 * base * height << endl;
    } else {
        cout << "invalid Choice" << endl;
    }
    return 0;
    
}








// sum and average of 3 numbers 
// take input from the users and find find sum and average

#include<iostream>
using namespace std;

int main() {
    int a, b, c, sum;
    float average;
    
    cin >> a >> b >> c;
    sum = a + b + c;
    average = sum / 3.0;
    
    cout << sum << endl;
    cout << average << endl;
     
    return 0;

}





//login crdential program
#include<iostream>
using namespace std;

int main() {
    const int userId = 1234;
    const int password = 0000;
    
    int inputID, inputpassword;
    cout << "Enter your ID here";
    cin >> inputID;
    cout << "Enter your password here";
    cin >> inputpassword;
    
    bool isGranted = (userId == inputID) && (password);
    
    cout << "Access Granted" << isGranted << endl;
    cout << "Access Denied" << !isGranted << endl;
    return 0;
    
}
